[project]
name = "memory-agent"
version = "0.1.0"
description = "Long-term memory dialog agent"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.30.0",
    "numpy",
    "sentence-transformers",
    "faiss-cpu",
]
